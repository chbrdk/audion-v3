/**
 * Plexon EQC / FastAPI-era clients POST `project_id` (snake_case).
 * AUDION Next contracts use `projectId`. Both must bind the TG (and thus personas) to the project.
 * @see apps/web/app/api/target-groups/route.ts
 * @see plexon-v3/lib/integrations/audion-persona-bootstrap-client.ts
 */
