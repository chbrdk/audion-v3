# Rail navigation icons

**Component:** `apps/web/components/nav-icons.tsx`  
**Wired in:** `apps/web/components/app-shell.tsx` (`PRIMARY_NAV`)

Local SVGs (Lucide-aligned) — no `lucide-react` in the rail (SSR-safe).

Order (top → bottom): Chat · Home · Projects · Personas · Target groups · Journeys · Studies.

| Nav item | Glyph concept |
|----------|----------------|
| Chat | Message bubble |
| Home | Layout dashboard |
| Projects | Folder |
| Personas | Single person (`User`) |
| Target groups | People group (`Users`) |
| Journeys | Route with start/end |
| Studies | Clipboard checklist |

Keep Personas vs Target groups visually distinct: one head vs group silhouette.
