# Magazine lede / filter / panel cutover (2026-08-03)

Uses `@msqdx/ui` primitives — see `msqdx-ui/knowledge/components/lede-filter-panel.md`.

- Wizards: `LedeStrip variant="steps"`
- Stats: `Lede` + `LedeStrip`
- Collection tiles: `Panel variant="card"`
- Aliases `WizardSteps` / `StatLede*` still exported from barrel for compat
