# audion-v3

Spec-driven AUDION rebuild on top of `@msqdx/ui`.

## Workspace

- `apps/web` — Next.js product UI
- `packages/contracts` — shared AUDION v3 contracts
- `specs/` — source of truth before implementation
- `knowledge/` — central paths and migration notes
