# Copy to .env.local (gitignored) and fill in keys.
OPENAI_API_KEY=
# Optional but strongly recommended for AgentOutput recovery:
# ANTHROPIC_API_KEY=

# Lab knobs (serve script also sets lean defaults)
# UX_JOURNEY_MAX_FAILURES=10
# UX_JOURNEY_OPENAI_MODEL=gpt-5.4-nano
# PORT=8320
